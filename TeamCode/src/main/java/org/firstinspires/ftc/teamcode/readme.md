## TeamCode (integration sandbox)

This module is the integration sandbox for slothboard — it exists so the dashboard library can be exercised against a real `@TeleOp`/`@Autonomous` OpMode during development.

It is not part of the published artifact; consumers depend on `com.github.6165-MSET-Cuttlefish.slothboard:dashboard:<ref>` instead.
